'use strict'
var test = require('tap').test
var streams = require('streams')
var util = require('util')
var Gauge = require('..')

function TestCollector () {
  stream.Writable.call(this)
  this.collected = ''
}
util.inherits(TestCollector, streams.Writable)
function _write(data, enc, cb) {
  this.collected += data.toString()
  cb()
}

var collector = new TestCollector()

var gauge = new Gauge(collector, {})

